<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dragonballz">
<!--    진동을 사용하겠다고 선언(Vibrator 사용 권한 획득)-->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".WinLoseRecord"
        android:allowBackup="true"
        android:icon="@drawable/dragon_ball"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BtnRecordActivity"></activity>
        <activity android:name=".BtnSetActivity"></activity>
        <activity android:name=".BtnStartActivity"></activity>

        <service android:name=".BackgroundMusic"></service>
    </application>

</manifest>